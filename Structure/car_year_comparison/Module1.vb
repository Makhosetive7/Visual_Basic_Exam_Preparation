Module Module1
    'Write a Structure named Car With Make (String), Model (String), And Year
    '(Integer). Implement a method called CompareByYear that takes another Car object And
    'returns an Integer indicating whether the current car Is older, newer, Or the same year As
    'the other car.

    Sub Main()

    End Sub

End Module
